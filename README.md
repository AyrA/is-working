# is-working

This essential module makes sure your JS engine is working.

## How to use

	var isWorking = require("is-working");
	if(isWorking()) {
		//Everything OK
	}
	else {
		//Something is wrong
	}
